# Production figure sources

Place production Typst sources in chapter directories such as
`chapter-01/`. Keep one standalone figure per `.typ` file and commit the
matching PDF under `../generated/` with the same chapter path and file stem.

The foundation pull request intentionally leaves this directory without any
book-figure conversions.
